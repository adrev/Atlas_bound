node_modules
*/node_modules
.git
.gitignore

# Build artifacts (we rebuild inside Docker)
shared/dist
server/dist
client/dist

# Development files
*.log
.env
.env.*
.DS_Store

# Data (SQLite DB — created at runtime, synced to Turso)
server/data

# User-uploaded content is included in the build
# (prebuilt token/map images are in client/public/)

# Editor config
.vscode
.idea
*.swp
*.swo
